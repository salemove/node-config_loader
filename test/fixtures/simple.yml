development:
  redis:
    host: localhost

production:
  redis:
    host: yawsah
  multiline: "one:\
              two"

flexible:
  foo: <%= process.env['foo'] %>
  undefined_val: <%= process.env['undefined_val'] %>
